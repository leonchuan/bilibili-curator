# 更新日志

本文件记录 bilibili-curator 的所有重要变更。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-11

首个公开发布版本。定位：**B站收藏夹治理**。

### 新增

**五个子命令**
- `export` — 导出全部收藏夹为 JSON（含 rid/bvid、标题、UP主、简介、时长、收藏时间）
- `stats` — 分析构成：各夹数量、失效条数、收藏年份分布、UP主 TOP15
- `analyze` — 反推各收藏夹主题画像（UP主分布 + TF-IDF 特征词 + 标题样本），只读、免登录
- `clean` — 批量删除已失效内容（投稿被 UP 删除），默认 dry-run
- `organize` — 把「待整理池」（默认收藏夹）的内容按主题归类。**只增不减**：已有分类夹里的条目一条不动，目标夹已存在则直接归入、不存在才新建；支持 `--source` 指定整理对象

**工程能力**
- `setup.py` 一键环境自举：创建虚拟环境、安装依赖、准备浏览器内核
- 浏览器三级回退：Playwright 自带 → 系统 Chrome → Edge → Chromium
- 零硬编码路径，全部从脚本位置推导；数据目录可用环境变量覆盖
- 全部写操作默认 dry-run，必须显式 `--commit` 才执行
- 跨平台支持 Windows / macOS / Linux，可选国内镜像源（`--mirror`）

### 技术要点

- 不使用「读取浏览器 cookie」方案：Chromium 127+ 的 App-Bound Encryption 已从根上阻断该路径
- 收藏内容删除接口为 `fav/resource/batch-del`（旧的 `del` 已废弃，返回 404），且需带 `platform=web`
- 移动接口 `fav/resource/move` 必须带 `mid` 参数，漏了会失败
- 登录态通过 Playwright 持久化 profile 保存，首次扫码后长期免登录

### 已知限制

- 失效内容只能靠标题特征识别（对已删稿件，接口返回「已失效视频」这类占位文案）
- 批量移动受 B站频率限制，千条规模约需 1-2 分钟
- 分类基于标题/简介关键词匹配，未命中的条目保持原位，不强行归类
- 删除不可撤销；移动可反向移回
